name: Grid Bot CI

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - '**'

jobs:
  lint:
    name: Code Quality Check
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      
      - name: Cache pip dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-lint-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-lint-
      
      - name: Install linting tools
        run: |
          python -m pip install --upgrade pip
          pip install flake8 black mypy
      
      - name: Run Black formatter check
        run: |
          black --check src/ tests/
      
      - name: Run Flake8 linter
        run: |
          flake8 src/ tes          flake8 src/ tes          flake8 src/ te03          flake8name: Run mypy           er
           : |
          mypy           mypymi        ports        rict-op    al 
  test  test  test  test  test  run  test  test  test  te  needs: l  test  
                         x:
                                  3.  ']
                                 ck                                 ck ou                                             at     yt                                 cks/setup-python@v5
        with:
          pytho          pytho          pyn-version }}
      
      - name: Cache pip dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ ru          key: ${{ ru          keyer          keyas          key: ${{ ru          key: ${{ restor          key: ${{ ru          keos }}-pip-${{ matrix.python-version }}-
      
      -  ame: Install       -  ame: Install       -  ame    pytho      -  ame: Install       - 
          pip in          pip in          pip in          pip in          pip in          pip in          pip in          pip in                 t > .env << 'ENVEOF'
          EXCHANGE_ID=binance
          MARKET_TYPE=spot
          API_KEY=test_api_key_placeholder
          API_SECRET=test_api_secret_placeholder
                                              OT_TOKEN=
          TELEGRAM_CHAT_ID=
          TESTNET=true
          ENVEOF
      
      - name: Create required directories
        run: |
          mkdir -p data/cache/ohlcv_cache
          mkdir -p data/state
          mkdir -p logs
      
      - name:      - name:      - name:      - nun      - name:      - nasts/ -v       - nam--c      - name:      - namemissing --cov-report=xml --cov-fail-under=80
      
      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report-${{ matrix.python-v    on           name: coverage-report-${{ matrix.python-v    on           name: coverage-report-${{age          name: coverage-report-${{ matrix.python-v    on           name: coverage-report-${{ matrix.python-v    on           name: coverage-report-${{age         on-          name: coverage-rep_i          name: coveragety-          name: coverage-report-${{ matrn:           name: coverage-report-${{ matrix.pyth            name: coverage-report-${{ matrix.python-v  ut@          name: covere: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
                                       ools
                              n             ll --upgrade pip
                                                                                                                                                                     eys..."
                                    /                                     /  de      AI API k                                    /                                     /  de      AI API k                                    /                                     /  de      AI API k                                    /                                     /  de      AI API k                                    /                                     /  de      AI API k                                    /                                     /  de      AI API k                                    /                                     /  de      AI API k                                               r                                         ts.txt
          pip-audit --desc
      
      - n      - n      - n      - n      - n      - n      - ncheck -      - n      - n      - n      - n      - n      - n      - ncheck -      - n      - n      - n      - n      - n      - n      - ncheck -      - n      - n      - n        
      - name: Upload security reports
        uses: actions/upload-artifact@v4
        with:
          name: security-reports
          path: |
            bandit-report.json
          retention-days: 30

  build-docker:
    name: Build Docker Image
    runs-on: ubuntu-latest
    needs: test
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          tags: grid-bot:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
